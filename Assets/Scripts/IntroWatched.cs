using UnityEngine;
using System.Collections;

public class IntroWatched : MonoBehaviour {


    public int introwatched;

	// Use this for initialization
	void Start () {
	/*	if (PlayerPrefs.GetInt("IntroWatched") == 1)
            this.gameObject.SetActive(false);
		else
	    	PlayerPrefs.SetInt("IntroWatched", 1);*/
    }
	
	
}
